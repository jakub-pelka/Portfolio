---
title: "REKWIZYTOR"
description: "System zarządzania rekwizytami dla produkcji teatralnych i filmowych — od fotografii do skatalogowanego zasobu w ciągu sekund, z pomocą Gemini Vision API."
facts:
  - label: "ROLA"
    value: "FULL-STACK DEVELOPER (SOLO)"
  - label: "KONTEKST"
    value: "INTERNAL TOOL / PROTOTYP"
  - label: "BACKEND"
    value: "NODE.JS / POSTGRES"
  - label: "COMMITY"
    value: "106"
  - label: "STATUS"
    value: "PROTOTYP GOTOWY"
process:
  - type: decision
    title: "GEMINI_VISION"
    content: "Wybrałem Gemini Vision zamiast custom modelu CV — lepiej rozumiał kontekst 'vintage aesthetic' i generował metadane które faktycznie mają znaczenie dla rekwizytora."
    anchor: "kod"
  - type: insight
    title: "WDROŻENIE"
    content: "Gotowy prototyp to za mało — wdrożenie zależy od decyzji organizacyjnych, nie od jakości kodu."
    anchor: "outcome"
  - type: milestone
    title: "PIPELINE"
    content: "Zbudowałem end-to-end pipeline: zdjęcie → Gemini Vision → Zod schema → Supabase — bez ręcznego parsowania odpowiedzi AI."
    anchor: "architektura"
---

<SectionHeader num="01" title="PROBLEM" />

<SplitLayout>
  <ContentLeft>
    <QuoteBlock>
      "Rekwizytorzy w produkcjach teatralnych i filmowych spędzają godziny na ręcznym katalogowaniu setek przedmiotów — każdy z opisem, stanem, lokalizacją i przypisaniem do spektaklu. To praca powtarzalna, podatna na błędy i trudna do przeszukiwania."
    </QuoteBlock>

    <BoxGrid>
      <ProcessBox title="REALNE ŹRÓDŁO">
        Projekt wyrósł z konkretnego problemu zgłoszonego przez rekwizytorów pracujących przy produkcjach teatralnych. Potrzeba była realna — wdrożenie nie doszło do skutku z powodów organizacyjnych.
      </ProcessBox>
      <ProcessBox title="HIPOTEZA">
        Jeśli zdjęcie wystarczy do identyfikacji — wystarczy też do katalogowania. Jeden obraz powinien generować pełen rekord bez ręcznego wprowadzania danych.
      </ProcessBox>
    </BoxGrid>
  </ContentLeft>

  <ContentRight>
    <TerminalCard title="PROJECT_INFO" status="active">
      <MetaRow label="STACK">
        <TagList>
          <Tag>Next.js 16</Tag>
          <Tag>Supabase</Tag>
          <Tag>Gemini Vision</Tag>
          <Tag>TypeScript</Tag>
          <Tag>Zod</Tag>
        </TagList>
      </MetaRow>
      <MetaRow label="COMMITY">106</MetaRow>
      <MetaRow label="ETAP">Prototyp — gotowy do wdrożenia</MetaRow>
    </TerminalCard>
  </ContentRight>
</SplitLayout>

<SectionHeader num="02" title="PRODUKT" id="produkt" />

<FullwidthImage
  src="/projects/rekwizytor/Home-page.png"
  alt="Rekwizytor — strona główna z listą spektakli"
  caption="Ekran główny — lista produkcji z przypisanymi rekwizytami i statusem inwentarza"
/>

<SplitLayout>
  <ContentLeft>
    <FullwidthImage
      src="/projects/rekwizytor/SpektaklList-page.png"
      alt="Lista spektakli"
      caption="Widok listy spektakli"
    />
  </ContentLeft>
  <ContentRight>
    <FullwidthImage
      src="/projects/rekwizytor/Grupy-page.png"
      alt="Grupy rekwizytów"
      caption="Grupowanie rekwizytów per produkcja"
    />
  </ContentRight>
</SplitLayout>

<SectionHeader num="03" title="ARCHITEKTURA" id="architektura" />

<CompareBlock beforeLabel="BEZ SYSTEMU" afterLabel="Z REKWIZYTOREM">
  <CompareRow label="Katalogowanie" before="Ręczne opisywanie każdego przedmiotu" after="Zdjęcie → automatyczny rekord przez Vision API" />
  <CompareRow label="Wyszukiwanie" before="Przeglądanie arkuszy Excel lub notatek" after="Filtrowanie po spektaklu, grupie, statusie" />
  <CompareRow label="Przypisanie" before="Ustne ustalenia, ryzyko pomyłki" after="Rekwizyt przypisany do sceny z historią zmian" />
  <CompareRow label="Raport PDF" before="Ręczne tworzenie przed każdą produkcją" after="Generowanie jednym kliknięciem" />
</CompareBlock>

<SectionHeader num="04" title="KOD" id="kod" />

<CodeSnippet
  lang="typescript"
  title="lib/vision/classify.ts"
  annotation="Gemini Vision API analizuje zdjęcie i zwraca ustrukturyzowany obiekt zgodny ze schematem Zod — bez ręcznego parsowania"
  code={"const schema = z.object({\n  name: z.string(),\n  category: z.enum(['furniture', 'clothing', 'prop', 'decoration']),\n  condition: z.enum(['new', 'good', 'worn', 'damaged']),\n  tags: z.array(z.string()),\n  notes: z.string().optional(),\n});\n\nexport async function classifyProp(imageBase64: string) {\n  const result = await model.generateContent([\n    { inlineData: { data: imageBase64, mimeType: 'image/jpeg' } },\n    CLASSIFY_PROMPT,\n  ]);\n  return schema.parse(JSON.parse(result.response.text()));\n}"}
/>

<CodeSnippet
  lang="typescript"
  title="app/api/props/route.ts"
  annotation="Upload zdjęcia → Supabase Storage → klasyfikacja Vision → zapis do bazy — jeden endpoint obsługuje cały pipeline"
  code={"export async function POST(req: Request) {\n  const form = await req.formData();\n  const file = form.get('image') as File;\n\n  const { path } = await supabase.storage\n    .from('props')\n    .upload(`${Date.now()}-${file.name}`, file);\n\n  const classification = await classifyProp(\n    await fileToBase64(file)\n  );\n\n  const { data } = await supabase\n    .from('props')\n    .insert({ ...classification, image_path: path })\n    .select()\n    .single();\n\n  return Response.json(data);\n}"}
/>

<OutcomeBanner statusTag="PROTOTYP_GOTOWY">
  System działa end-to-end — od uploadu zdjęcia do przeszukiwalnego katalogu. Prototyp był demonstrowany zainteresowanym rekwizytorkom, jednak wdrożenie nie doszło do skutku z powodów organizacyjnych. Architektura jest gotowa na produkcję.
</OutcomeBanner>
